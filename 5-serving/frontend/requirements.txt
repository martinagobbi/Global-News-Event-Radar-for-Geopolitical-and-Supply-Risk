# Pinned exactly — see 3-validation_and_storage/requirements.txt for why an
# uncommitted ~=X tightening on this file would have downgraded a currently
# running, already-tested environment on the next rebuild. Versions below are
# what THIS image actually resolves to today (2026-08-25), confirmed via
# `pip freeze`, not guessed.
streamlit==1.61.1
pandas==3.0.5
pydeck==0.9.3
requests==2.34.2